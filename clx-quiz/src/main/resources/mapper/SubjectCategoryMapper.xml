<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.quiz.mapper.SubjectCategoryMapper">

    <resultMap id="CategoryResultMap" type="com.clx.quiz.entity.SubjectCategory">
        <id column="id" property="id"/>
        <result column="category_name" property="categoryName"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_num" property="sortNum"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <select id="selectAll" resultMap="CategoryResultMap">
        SELECT * FROM subject_category
        WHERE is_deleted = 0
        ORDER BY sort_num, id
    </select>

    <select id="selectById" resultMap="CategoryResultMap">
        SELECT * FROM subject_category
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO subject_category (category_name, parent_id, sort_num, created_by)
        VALUES (#{categoryName}, #{parentId}, #{sortNum}, #{createdBy})
    </insert>

    <update id="update">
        UPDATE subject_category
        SET category_name = #{categoryName},
            parent_id = #{parentId},
            sort_num = #{sortNum},
            update_by = #{updateBy}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE subject_category
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>