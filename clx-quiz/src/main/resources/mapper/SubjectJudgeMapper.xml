<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.quiz.mapper.SubjectJudgeMapper">

    <resultMap id="JudgeResultMap" type="com.clx.quiz.entity.SubjectJudge">
        <id column="id" property="id"/>
        <result column="subject_id" property="subjectId"/>
        <result column="is_correct" property="isCorrect"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO subject_judge (subject_id, is_correct, created_by)
        VALUES (#{subjectId}, #{isCorrect}, #{createdBy})
    </insert>

    <select id="selectBySubjectId" resultMap="JudgeResultMap">
        SELECT * FROM subject_judge
        WHERE subject_id = #{subjectId} AND is_deleted = 0
    </select>

    <update id="deleteBySubjectId">
        UPDATE subject_judge SET is_deleted = 1 WHERE subject_id = #{subjectId}
    </update>

</mapper>