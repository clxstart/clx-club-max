<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.user.mapper.PostFavoriteMapper">

    <resultMap id="FavoriteResultMap" type="com.clx.user.entity.PostFavorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="post_id" property="postId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByUserAndPost" resultMap="FavoriteResultMap">
        SELECT id, user_id, post_id, create_time
        FROM post_favorite
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <insert id="insert">
        INSERT INTO post_favorite (user_id, post_id, create_time)
        VALUES (#{userId}, #{postId}, NOW())
    </insert>

    <delete id="deleteByUserAndPost">
        DELETE FROM post_favorite
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="selectPostIds" resultType="long">
        SELECT post_id
        FROM post_favorite
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM post_favorite WHERE user_id = #{userId}
    </select>

</mapper>