spring:
  # Redis（Docker容器）
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 2
      lettuce:
        pool:
          max-active: 200
          max-idle: 50
          min-idle: 10